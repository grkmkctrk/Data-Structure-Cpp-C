#include "../Inc/XORLL.h"

int main(){

    // create a new node
    List list = newList(list);
    
    insert(&list, 4);
    insert(&list, 11);
    insert(&list, 7);
    insert(&list, 9);
    insert(&list, 2);

    print(list, pf);

    return 0;
}